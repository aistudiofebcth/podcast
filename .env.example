# LINE LIFF app id — from the LINE Developers console (LINE Login channel → LIFF).
# Used by the podcast LIFF app (liff.html). Leave empty to preview standalone
# in a normal browser; set it for the deployed build so liff.init() runs.
VITE_LIFF_ID=
